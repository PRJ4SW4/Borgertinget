# BackendMappe/.env.example - Skabelon til nødvendige miljøvariabler.
# Udfyld delene med passwords, med vores reelle passwords i en ny .env fil i opretter
# 

# --- Hemmeligheder til Docker Compose Services ---
# Bruges direkte i docker-compose.yml via ${...}
DB_USER=borgertinget
DB_PASSWORD=YourDatabasePasswordGoesHere # Indtast det korrekte udviklings-password
DB_NAME=borgertinget
PGADMIN_EMAIL=borgertinget@admin.com
PGADMIN_PASSWORD=YourPgAdminPasswordGoesHere # Indtast det korrekte pgAdmin password

# --- Hemmeligheder til .NET Backend Applikationen ---
#Mindre test for at se om gitignore fungerer
# Læses af DotNetEnv og mappes til IConfiguration via Section__Key format
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD} # Denne genbruger variabler ovenfor
Jwt__Key=YourJwtSecretKeyGoesHere # Indtast den lange JWT nøgle
Email__AppPassword=YourEmailAppPasswordGoesHere # Indtast Gmail App Password eller lignende
GoogleOAuth__ClientSecret=YourGoogleClientSecretGoesHere # Indtast Google OAuth Client Secret